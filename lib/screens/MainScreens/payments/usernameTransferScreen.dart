import 'package:flutter/material.dart';


class UsernameTransferScreen extends StatelessWidget {
  const UsernameTransferScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: ,
    );
  }
}